spring:
  application:
    name: pis
  server:
    port: ${SERVER_PORT:8080}
  datasource:
    url: ${MySQL_JDBC_URL:jdbc:mysql://localhost:3309/pis}
    username: ${MySQL_USER:root}
    password: ${MySQL_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        use_sql_comments: true
    open-in-view: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME:ngocuyenle18@gmail.com}
    password: ${EMAIL_PASSWORD:yzjqdenwoonhtpaj}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com